[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "autocam"
version = "0.1.0"
description = "AutoCAM Flask app"
authors = [{ name = "AutoCAM" }]
requires-python = ">=3.11" 